import { SlideInOnScrollDirective } from './slide-in-on-scroll.directive';

describe('SlideInOnScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new SlideInOnScrollDirective();
    expect(directive).toBeTruthy();
  });
});
